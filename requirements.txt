# Единственная внешняя зависимость всего проекта — клиент OpenAI,
# который использует Stage 2 (pipeline/stage2_llm_analyze.py) для разбора
# описаний объявлений. Всё остальное (оркестратор, Stage 3, очистка) —
# чистая стандартная библиотека Python: asyncio, csv, json, urllib.
#
# Версия зафиксирована точно, чтобы деплой был воспроизводимым: Stage 2
# вызывает chat.completions с параметрами reasoning_effort и
# max_completion_tokens, и молчаливая смена мажорной версии SDK способна
# их сломать. Поднимать — осознанно и с проверкой прогона.
openai==3.16.2
